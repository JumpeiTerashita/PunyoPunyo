#pragma once

#include "Puyo.h"

extern int ColChanger(COLORPATTERN _color);

extern unsigned int ColBitPicker(COLORPATTERN _color,unsigned int _lookat);

extern unsigned int plusResultBit(COLORPATTERN _color,const unsigned int _lookat, const unsigned int _add);

